Fix the bug in `derrick stack submit` where it calls `transition_to_in_review` on tickets that are already in `in_review` state, causing the substrate to reject the transition. The fix should make `stack submit` idempotent — if a ticket is already `in_review`, skip the transition and just open (or update) the PR. If it's already `in_review` and already has a `pr_url`, skip entirely or re-use the existing PR number. Find the relevant code in the stack submit command/handler, patch it, add a test covering the idempotent case, build clean, tests pass, then commit: `fix(stack): make stack submit idempotent on in_review tickets`.