//! Built-in per-tool scrub rule sets.

/// Rules for `bd`.
pub mod bd;
/// Rules for Cargo.
pub mod cargo;
/// Rules for Claude Code.
pub mod claude;
/// Rules for Codex.
pub mod codex;
/// Rules for Copilot.
pub mod copilot;
/// Rules for `gh`.
pub mod gh;
/// Rules for `git`.
pub mod git;
/// Rules for `gt`.
pub mod gt;
